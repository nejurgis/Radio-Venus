{
  "name": "radio-venus",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:db": "node scripts/build-db.mjs",
    "db:import": "node scripts/db-import.mjs",
    "db:stats": "node scripts/db-stats.mjs",
    "preview": "vite preview",
    "deploy": "vite build && npx gh-pages -d dist"
  },
  "dependencies": {
    "astronomy-engine": "^2.1.0",
    "yt-search": "^2.13.1"
  },
  "devDependencies": {
    "better-sqlite3": "^12.6.2",
    "vite": "^6.0.0"
  }
}
